name: Environment
on: 
    workflow_dispatch:
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Print Env Build
                run: |
                    echo ""
                    echo ""

