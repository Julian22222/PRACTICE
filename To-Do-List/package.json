{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client-front-end && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client-front-end && npm start"
  },
  "keywords": [],
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
