{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "MySettings": "This is My Settings from appsettings.json",
  "MainSettings": {
    "SubSettings": "This is my SubSettings from appsettings.json"
  },
  "ConnectionStrings": {
    "Default": "This is connection string should be in User Secrets file(secrets.json), in Environmant variables but not in appsettings.json file"
  }
}
